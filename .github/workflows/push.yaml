---
name: Continous Integration Pipeline

on:
  workflow_dispatch:
  push:
   branches: '*'

  pull_request:
    branches: 'master'

jobs: 
  run-jest-tests:
    uses: ./.github/workflows/test_frontend.yaml
